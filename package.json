{
  "name": "vscode-mcp-hook",
  "displayName": "VSCode MCP Hook",
  "description": "Per-window MCP server that terminals in this VSCode window can connect to.",
  "version": "0.1.4",
  "publisher": "you",
  "engines": {
    "vscode": "^1.116.0"
  },
  "categories": [
    "Other"
  ],
  "activationEvents": [
    "onStartupFinished"
  ],
  "main": "./dist/extension.js",
  "contributes": {
    "mcpServerDefinitionProviders": [
      {
        "id": "vscode-mcp-hook.server",
        "label": "VSCode MCP Hook"
      }
    ],
    "commands": [
      {
        "command": "vscodeMcpHook.showInfo",
        "title": "MCP Hook: Show Connection Info"
      },
      {
        "command": "vscodeMcpHook.writeWorkspaceConfig",
        "title": "MCP Hook: Write MCP config files for CLI clients"
      }
    ]
  },
  "scripts": {
    "lint": "eslint src tests --ext .ts",
    "compile": "tsc -p ./ --noEmit",
    "bundle": "esbuild src/extension.ts --bundle --outfile=dist/extension.js --external:vscode --format=cjs --platform=node --sourcemap",
    "watch": "npm run bundle -- --watch",
    "test": "jest",
    "package": "vsce package --allow-missing-repository --baseContentUrl https://github.com/placeholder/vscode-mcp-hook --out dist",
    "package:local": "pwsh ./scripts/build-package.ps1",
    "build": "npm run lint && npm run compile && npm run bundle && npm run package",
    "vscode:prepublish": "npm run bundle"
  },
  "devDependencies": {
    "@types/jest": "^30.0.0",
    "@types/node": "^20.11.0",
    "@types/vscode": "^1.116.0",
    "@typescript-eslint/eslint-plugin": "^8.59.0",
    "@typescript-eslint/parser": "^8.59.0",
    "@vscode/vsce": "^3.9.1",
    "esbuild": "^0.28.0",
    "eslint": "^10.2.1",
    "jest": "^30.3.0",
    "ts-jest": "^29.4.9",
    "typescript": "^5.3.3"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.4",
    "zod": "^3.22.4"
  },
  "overrides": {
    "ip-address": "^10.1.1",
    "hono": ">=4.12.16"
  }
}
